{
  "description": "Check for jdtls installation on session start",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "powershell -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/check-jdtls.ps1\"",
            "timeout": 60
          }
        ]
      }
    ]
  }
}