{
  "description": "Check for dockerfile-language-server installation on init",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/check-dockerfile-language-server.ps1\"",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
