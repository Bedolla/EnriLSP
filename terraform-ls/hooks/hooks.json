{
  "description": "Check for terraform-ls installation on init",
  "hooks": {
    "Setup": [
      {
        "matcher": "init",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/check-terraform-ls.ps1\"",
            "timeout": 300
          }
        ]
      }
    ]
  }
}
