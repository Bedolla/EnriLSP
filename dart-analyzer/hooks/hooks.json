{
  "description": "Check for Dart SDK installation on init",
  "hooks": {
    "Setup": [
      {
        "matcher": "init",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/check-dart-analyzer.ps1\"",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
