{
  "description": "Check for pyright installation on init",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/check-pyright.ps1\"",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
