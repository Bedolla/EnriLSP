{
  "description": "Check for lua-language-server installation on init",
  "hooks": {
    "Setup": [
      {
        "matcher": "init",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/check-lua-language-server.ps1\"",
            "timeout": 300
          }
        ]
      }
    ]
  }
}
