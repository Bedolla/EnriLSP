{
  "description": "Check for rust-analyzer installation on init",
  "hooks": {
    "Setup": [
      {
        "matcher": "init",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/check-rust-analyzer.ps1\"",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
