{
  "description": "Check for rust-analyzer installation on init",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/check-rust-analyzer.ps1\"",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
